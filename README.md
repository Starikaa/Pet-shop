put them all in a single folder. Change direction to backend, open it in terminal, input node server.js. Change direction to frontend, open it in terminal, input npm start. Make sure you have connection to the database and a chatbot API and a cloudinary API
What to put in .env file: 
PORT=5000
SQL_USER=
SQL_PASSWORD=
SQL_DATABASE=PetShopManagement
SQL_SERVER=localhost
GEMINI_API_KEY=
